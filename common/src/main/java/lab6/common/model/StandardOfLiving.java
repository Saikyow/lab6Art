package lab6.common.model;

/**
 * Уровень жизни в городе.
 * Порядок констант задаёт естественное сравнение (NIGHTMARE — самый низкий).
 */
public enum StandardOfLiving {
    NIGHTMARE,
    ULTRA_LOW,
    LOW
}
